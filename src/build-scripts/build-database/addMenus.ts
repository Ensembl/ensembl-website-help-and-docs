// import { Menu } from '../../models';

// import { Menus } from './buildMenus';

// const addMenus = async (menus: Menus) => {
//   for (const menu of menus) {
//     await Menu.create({
//       name: menu.name,
//       data: JSON.stringify(menu.data)
//     });
//   }
// };

// export default addMenus;
